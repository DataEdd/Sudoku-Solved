# Virtual environment
venv/

# Claude Code local settings
.claude/
CLAUDE.md

# Python
__pycache__/
*.py[cod]
*.so
.Python
.pytest_cache/
.ruff_cache/
*.ipynb_checkpoints/

# Environment
.env
.env.local

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Uploads / temp files
uploads/
*.tmp
TODO

# Track only the ~2 MB Ground Example GT set under Examples/; the aug/
# directory and unsolved_dataset.zip stay out of git — bulk training artefacts,
# not part of the shipped benchmark dataset.
Examples/*
!Examples/Ground Example/

# Generated artifacts
output/
reports/
tests/output/

# Internal project notes (not part of public repo)
docs/internal/
docs/data/

# Local dataset staging / prior Kaggle-publishing workspace
data/

# OCR v2 sub-project scaffolding (private, in-progress)
ocr_v2/

# Notebooks: only top-level pipeline deep-dive notebooks + README are
# tracked. Any subdir under notebooks/ is local-only (restore from git
# history for reference).
notebooks/*/

# Reference papers (license-restricted PDFs stay out of the repo)
research/

# Versioned CNN checkpoint backups (local rollback artifacts)
app/ml/checkpoints/sudoku_cnn.v*
app/ml/checkpoints/sudoku_cnn.pre-redesign*
app/ml/checkpoints/training_results.v*
app/ml/checkpoints/training_results.pre-redesign*
app/ml/*.log

# CV sync — local notes, never commit
CV_NOTES.md

# API tokens — never commit
api.md
kaggle.json
*/dataset-metadata.json

# Local README draft during iteration
README_draft.md
